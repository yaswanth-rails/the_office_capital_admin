<%= form_for @user ,url:kyc_verified_edit_path, method: :post, local: true, data: { turbo: false } do  |form|%>
	<div class="form-group row">
		<label for="kyc_verified" class="col-sm-2 col-form-label">Kyc Verified</label>
		<div class="col-sm-6">
			<%= form.check_box :kyc_verified%>
		</div>
		<%if @user.kyc_verified !=true || current_toc.role.role!="employee"%>
		<br>
		<br>
		<div class="col-sm-2">
		</div>
		<div class="col-sm-6">
			<%= form.submit "Submit",class: "btn btn-primary btn-md"%>
		</div>
		<%end%>
	</div>
<%end%>